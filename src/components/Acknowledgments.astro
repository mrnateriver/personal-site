---
import AstroLogo from './AstroLogo.astro';
import GitHubLogo from './GitHubLogo.astro';
import SectionLink from './SectionLink.astro';

const { class: className } = Astro.props;
---

<section class:list={['acks-container', className]}>
  <p class="copyrights">© {new Date().getFullYear()} Evgenii</p>
  <div class="stack">
    <SectionLink
      class="stack-entry stack-astro"
      href="https://astro.build"
      borderOffset={0}
      borderSize={8}
      borderWidth={4}
    >
      <span>Made with CSS and</span>
      <AstroLogo color="var(--color-text-primary)" width={75} height={25} />
    </SectionLink>
    <SectionLink
      class="stack-entry stack-github"
      href="https://github.com/mrnateriver/personal-site"
      borderOffset={0}
      borderSize={8}
      borderWidth={4}
    >
      <span>Source code</span>
      <GitHubLogo color="var(--color-text-primary)" width={20} height={20} />
    </SectionLink>
  </div>
</section>

<style lang="scss">
  .acks-container {
    font-family: 'Handjet', cursive;
    font-size: var(--font-size-lg);
    font-weight: bold;
  }

  .stack {
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: center;
    gap: var(--padding-lg);

    @media (max-width: 650px) {
      flex-direction: column;
    }

    &-entry {
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: right;
      position: relative;
      gap: var(--padding-md);
      padding: 0 var(--padding-lg);
      color: var(--color-text-primary);
      height: var(--font-size-xl);
    }
  }

  .copyrights {
    font-weight: normal;
    text-align: center;
    margin-bottom: var(--padding-sm);
    color: var(--color-text-tertiary);
  }
</style>
